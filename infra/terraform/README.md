# Terraform Skeleton

Modules (planned):
- network
- compute (ECS Fargate for API/Web; GPU EC2 ASG for workers)
- storage (S3 buckets with lifecycle)
- queue (SQS with priority)
- database (RDS Postgres)
- cdn (CloudFront) & waf

This is a scaffold; resources to be added in subsequent sprints.
